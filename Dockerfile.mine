ARG BASE_IMAGE
FROM ${BASE_IMAGE}

# Install additional ROS/utility packages
RUN apt-get update && apt-get install -y \
    ros-humble-joy \
    ros-humble-robot-localization \
    nautilus \

&& rm -rf /var/lib/apt/lists/* \
&& apt-get clean

